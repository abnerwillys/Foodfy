{% extends 'layout.njk' %}

{% block head %}
  <title>FoodFy | Home</title>

  <link rel="stylesheet" href="/styles/partials/clientArea/index.css">
  <link rel="stylesheet" href="/styles/partials/clientArea/cards.css">
{% endblock %}

{% block content %}

  <section id="box-introduction">
    <div class="text-container">
      <h2>As melhores receitas</h2>
      <p>
        Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.
      </p>
    </div>

    <div class="image-container">
      <img src="assets/chef.png" alt="Chef">
    </div>
  </section>

  <section id="more-access">

    <h3>Mais acessadas</h3>

    <div class="cards">

      {% for recipe in recipes %}
        {% if loop.index <= 6 %}
          <div class="card" data-id={{recipe.id}}>
            <div class="recipe-image" style="background-image: url({{ recipe.image }})"></div>
            <p class="food-name">{{ recipe.title }}</p>
            <p class="by-chef">por {{ recipe.chef_name }}</p>
          </div>
        {% endif %}
      {% endfor %}

    </div>
  </section>

{% endblock %}

{% block script %}
  <script src="/scripts/clientArea/click-recipes.js"></script>
{% endblock %}